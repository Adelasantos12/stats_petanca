DATABASE_URL="postgresql://user:password@localhost:5432/petanca?schema=public"
PORT=4000

# Autenticación (coach / super-admin)
JWT_SECRET="cambia-esto-por-un-secreto-largo-y-aleatorio"
JWT_EXPIRES_IN="30d"

# Login con Google (opcional). Si se deja vacío, el botón de Google se oculta
# y solo funciona el login por email/contraseña.
# Crea un "OAuth Client ID" (tipo Web) en https://console.cloud.google.com/apis/credentials
GOOGLE_CLIENT_ID=""
